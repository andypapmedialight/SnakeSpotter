# Optional. Without this key the app stays usable:
# you log coordinates by hand and a banner explains that the map is offline.
GOOGLE_MAPS_API_KEY=

# Optional. Empty keeps admin off. Set a password to sign in and delete sightings.
ADMIN_PASSWORD=
